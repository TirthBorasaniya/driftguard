# Python 3.11 strictly

# Core ML
lightgbm==4.6.0
scikit-learn==1.4.2
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
shap==0.45.1
scipy==1.13.0

# ONNX export and inference
onnxmltools==1.16.0
skl2onnx==1.20.0
onnxruntime==1.27.0

# Experiment tracking
mlflow==2.12.2
# setuptools upper bound is required, lower bound is a security fix.
# Upper: mlflow 2.12.2 does `import pkg_resources` in
# mlflow/utils/requirements_utils.py, and pkg_resources was removed from
# setuptools in 81.0.0, so >=81 breaks mlflow with ModuleNotFoundError.
# Lower: >=78.1.1 fixes CVE-2024-6345 (package_index RCE, fixed 70.0.0) and
# CVE-2025-47273 (path traversal, fixed 78.1.1). The previous `setuptools<70`
# pin blocked both fixes for no stated reason. Verified: 80.10.2 passes the
# full suite (93 passed, 1 skipped, identical to the old pin).
setuptools>=78.1.1,<81

# Orchestration
prefect==2.19.7
griffe<1.0

# Feature store
feast==0.40.0
redis==5.0.4

# Data validation
great-expectations==0.18.19

# Streaming
confluent-kafka==2.4.0
fastavro==1.9.4

# Serving
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.1
pydantic-settings==2.3.0
aiosqlite==0.20.0

# Drift monitoring - pinned exactly, 0.5+ breaks API
evidently==0.4.30

# Infrastructure monitoring
prometheus-fastapi-instrumentator==6.1.0
prometheus-client==0.20.0

# Testing
pytest==8.2.0
httpx==0.27.0
pytest-asyncio==0.23.7

# Linting / type checking
ruff==0.4.7
mypy==1.10.0

# Utilities
python-dotenv==1.0.1
requests==2.32.3
pyarrow==15.0.2
locust==2.28.0
