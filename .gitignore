# Data — tracked separately (too large for git)
data/raw/
data/processed/
data/features/

# Encoders and model artifacts — commit these for deployment
# data/encoders/   <- intentionally NOT ignored
# models/          <- intentionally NOT ignored

# MLflow
mlruns/
mlflow.db

# Feast registry (auto-generated by feast apply)
data/feast_registry.db
src/features/feature_repo/data/

# Predictions database (runtime state, not for git)
predictions.db

# Evidently reports (regenerated on demand)
reports/*.html

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# IDE
.vscode/
.idea/
